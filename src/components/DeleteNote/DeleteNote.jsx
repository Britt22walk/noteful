import React from 'react';

const DeleteNote = (props) => {
    console.log(props)
    return (
<button type="delete" onClick={(e)=>{}}>Delete This Note</button>
      );
}
 
export default DeleteNote;